rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;
      // MVP: allow writes so the supervisor view can save without auth.
      // Replace with auth or server-side validation before production.
      allow write: if true;
    }
  }
}
